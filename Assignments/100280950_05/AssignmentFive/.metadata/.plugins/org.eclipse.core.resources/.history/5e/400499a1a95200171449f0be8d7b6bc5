/**
 * 
 * @author jab
 *
 */
public class TableTester {
	/**
	 * 
	 * @param args
	 */
	public static void main(String[] args) {
		Table table = new Table(5, 5);
		
		for (int row = 0; row < 5; row++)
		{
			for (int col = 0; col < 5; col++)
			{
				table.set(9, row, col);
				//System.out.println("[" + row + "][" + col + "]");
			}
		}
		
		System.out.println(table.sum(3, false));
	}
}