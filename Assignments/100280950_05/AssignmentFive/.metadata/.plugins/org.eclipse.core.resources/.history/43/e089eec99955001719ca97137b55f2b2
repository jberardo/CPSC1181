import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class MagicSquare {

	public static void main(String[] args) {
		final int MAX_NUMBERS = 16;
		final int NUM_ROWS = 4;
		final int NUM_COLS = 4;
		final int NUM_DIAG = 2;
		
		File file = new File("input.txt");
		Scanner input = null;
		Table square = new Table(NUM_ROWS, NUM_COLS, true);
		int sumOfRows = 0;
		int sumOfCols = 0;
		int sumOfDiagonals = 0;

		try {
			input = new Scanner(file);
		} catch (FileNotFoundException e) {
			System.out.println("Error opening file.");
			System.exit(1);
		}
		
		for (int row = 0; row < NUM_ROWS; row++)
		{
			for (int col = 0; col < NUM_COLS; col++)
			{
				square.set(row, col, input.nextInt());				
			}
		}
		
		for (int i = 0; i < NUM_ROWS; i++)
		{
			sumOfRows = (int) square.sum(i, true);
		}
		
		for (int i = 0; i < NUM_COLS; i++)
		{
			sumOfCols = (int) square.sum(i, false);
		}
		
		for (int i = 0; i < NUM_DIAG; i++)
		{
			sumOfDiagonals = (int) square.getDiagonalSum(i);
		}
		
		System.out.println("Sum of rows: " + sumOfRows);
		System.out.println("Sum of cols: " + sumOfCols);
		System.out.println("Sum of diags: " + sumOfDiagonals);
	}
}