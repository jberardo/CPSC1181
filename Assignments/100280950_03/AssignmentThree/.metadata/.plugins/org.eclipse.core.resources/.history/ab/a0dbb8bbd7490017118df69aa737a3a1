/**
 * Formats a string containing 10 numbers into a phone number
 * Format: (000) 555-5555
 * 
 * @author Joao Berardo
 * @version 1.0
 * @since June 04, 2017
 *
 */
public class FormatPhoneNumber {
	// attributes
	private String rawNumber = "";
	
	/**
	 * Constructor
	 * @param number String containing 10 numbers to be formatted
	 */
	public FormatPhoneNumber(String number) {
		rawNumber = number;
	}
	
	public String formatNumber() {
		// Area code: substring of first 3 chars and add ()
		String areaCode = "(" + rawNumber.substring(0, 3) + ") ";

		// Formatted number: 
		// Concatenate area code
		// substring next three characters
		// a hyphen
		// and the substring consisting of the last four characters
		
		return "";
	}
}